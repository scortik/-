cvet=0
i = 0
def setup():
    size(600,600)
def draw():
    global cvet,i
    i=i+1
    #for x in range(0,600,20):
    #line(x,200-x,x,300)
    for y in range(15):
        push()
        for x in range(15):
            if cvet == 0:
                cvet=255
            else:
                cvet=0 
            translate(60,0)
            ellipse(-60,-60,60,60)
            fill(cvet)       
        pop()
        translate(0,60)
    # for m in range(4):
        # push()
        # for t in range(
